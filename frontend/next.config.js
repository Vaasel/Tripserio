/** @type {import('next').NextConfig} */

module.exports = {
  images: {
    domains: ["images.unsplash.com"], // Add any other domains as needed
    loader: "default", // Or use 'imgix' or another loader if preferred
  },
};
